<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Teklif" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:statusBarColor">@color/brand</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>

    <!-- Uygulama acilirken gosterilen karsilama ekrani. -->
    <style name="Theme.Teklif.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/brand</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Teklif</item>
        <item name="android:statusBarColor">@color/brand</item>
    </style>
</resources>
